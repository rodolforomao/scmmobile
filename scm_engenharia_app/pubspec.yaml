name: scm_engenharia_app
description: A SCM Engenharia oferece consultoria especializada sobre legislação, regulamentação e fiscalização da atividade no setor de telecomunicação
version: 0.1.0
build: 10000
homepage: https://www.scmengenharia.com.br
repository: https://www.scmengenharia.com.br

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html


environment:
  sdk: ">=2.17.5 <3.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.

  cupertino_icons: ^1.0.5 # ANDROID, IOS, LINUX, MACOS, WEB, WINDOWS
  animations: ^2.0.4 # ANDROID, IOS, LINUX, MACOS, WEB, WINDOWS
  image_picker: ^0.8.5+3  # ANDROID, IOS, WEB
  path_provider: ^2.0.11 # ANDROID, IOS, LINUX, MACOS, WINDOWS
  shared_preferences: ^2.0.15 # ANDROID, IOS, LINUX, MACOS, WEB
  webview_flutter: ^3.0.4 # ANDROID, IOS
  url_launcher: ^6.1.5 # ANDROID, IOS, LINUX, MACOS, WEB, WINDOWS
  file_selector: ^0.9.0 #  MACOS, WEB , WINDOWS

  #Plugin microsoft
  fluentui_system_icons: ^1.1.178 # ANDROID ,IOS, LINUX, MACOS, WEB, WINDOWS

  #Plugin dart.dev
  http: ^0.13.5 # ANDROID, IOS, LINUX, MACOS, WEB, WINDOWS
  mockito: ^5.3.0 # ANDROID, IOS, LINUX, MACOS, WEB, WINDOWS
  intl: ^0.17.0 # ANDROID, IOS, LINUX,MACOS, WEB, WINDOWS
  html: ^0.15.0 # ANDROID, IOS, LINUX,MACOS, WEB, WINDOWS

  #Plugin tekartik.com
  realm: ^0.4.0+beta # ANDROID, IOS, LINUX, MACOS, WINDOWS

  #Plugin Flutter Community
  connectivity_plus: ^2.3.6+1 # ANDROID, IOS, LINUX, MACOS, WEB, WINDOWS
  device_info_plus: ^4.1.2 # ANDROID, IOS, LINUX, MACOS, WEB, WINDOWS
  package_info_plus: ^1.4.3+1 # ANDROID, IOS, LINUX, MACOS, WEB, WINDOWS
  share_plus: ^4.0.10+1 # ANDROID, IOS, LINUX, MACOS, WEB, WINDOWS

  #Plugin google
  file: ^6.1.2 # ANDROID, IOS, LINUX, MACOS, WINDOWS

  #Plugin firebase
  firebase_core: ^1.21.1 # ANDROID ,IOS, MACOS, WEB,
  firebase_crashlytics: ^2.8.9 # ANDROID ,IOS, MACOS
  firebase_messaging: ^13.0.0 # ANDROID,IOS, MACOS, WEB
  firebase_analytics: ^9.3.3 # ANDROID,IOS,MACOS,WEB

  #Plugin outros
  jaguar_jwt: ^3.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/img/logo_white.png # logo negativa em branco
    - assets/img/ic_whatsapp.png
    - assets/img/img_error.png
    - assets/img/img_integracao.png
    - assets/img/img_sem_sinal.png
    - assets/img/img_informacao.png
    - assets/variavel_de_ambiente.json

  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    # -------------------------------------------------------------------------------  textButtonTheme
    - family: avenir-lt-std-roman
      fonts:
        - asset: assets/fonts/avenir_lt/avenir-lt-std-roman.otf
    # -------------------------------------------------------------------------------
    - family: avenir-next-rounded-pro-medium-lt
      fonts:
        - asset: assets/fonts/avenir_next/avenir-next-rounded-pro-medium-lt.ttf
    # ------------------------------------------------------------------------------- AppBar
    - family: avenir-lt-std-roman
      fonts:
        - asset: assets/fonts/avenir_lt/avenir-lt-std-roman.otf
    # -------------------------------------------------------------------------------
    - family: avenir-lt-std-medium
      fonts:
        - asset: assets/fonts/avenir_lt/avenir-lt-std-medium.otf
    # ------------------------------------------------------------------------------- inputDecorationTheme
    - family: avenir-lt-std-book-oblique
      fonts:
        - asset: assets/fonts/avenir_lt/avenir-lt-std-book-oblique.otf
    # -------------------------------------------------------------------------------
    - family: avenir-lt-std-book
      fonts:
        - asset: assets/fonts/avenir_lt/avenir-lt-std-book.otf
    # -------------------------------------------------------------------------------


  # fonts:
  #   - family: Schyler open-sans-regular
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
